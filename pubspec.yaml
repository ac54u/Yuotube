name: flutter_application_youtube
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # ------------------------------------------------
  # 🔥 核心业务库
  # ------------------------------------------------
  
   # 🔥 强制使用 GitHub 最新版，修复 VideoUnavailable 报错
  youtube_explode_dart:
    git:
      url: https://github.com/Hexer10/youtube_explode_dart.git
      ref: master

  
  # 视频转码 (4K 下载合成)
  ffmpeg_kit_flutter_new: ^4.1.0
  
  # 浏览器内核 (保留作为备用方案)
  flutter_inappwebview: ^6.0.0
  
  # AI 翻译
  translator: ^1.0.0

  # ------------------------------------------------
  # 🎬 播放器引擎 (MediaKit - Infuse 级内核)
  # ------------------------------------------------
  # 核心逻辑
  media_kit: ^1.1.10
  # 视频渲染控制
  media_kit_video: ^1.2.4
  # iOS/Android 原生解码库 (必须包含这个才能硬解 4K)
  media_kit_libs_video: ^1.0.4

  # 屏幕常亮
  wakelock_plus: ^1.2.0

  # ------------------------------------------------
  # 🛠️ 系统能力 & 工具
  # ------------------------------------------------
  
  # 权限管理
  permission_handler: ^11.3.1
  
  # 保存到相册
  gal: ^2.3.2
  
  # 分享
  share_plus: ^10.0.0
  
  # 图片选择
  image_picker: ^1.0.7
  
  # 本地存储
  shared_preferences: ^2.2.2
  
  # 网络请求
  http: ^1.2.0
  
  # 图片缓存
  cached_network_image: ^3.4.1

  # 图标
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
